from svmath import *

width = Fixed(8, 0, "width")
height = Fixed(8, 0, "height")
pos = Vec2(7, 25, "pos")
vel = Vec2(6, 26, "vel")
rotation = Fixed(3, 7, "rotation")

#obb_reg = SVModule("obb_reg", inputs=[], outputs=[width, height, pos, vel, rotation])